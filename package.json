{
  "name": "ares-complete-06",
  "version": "1.0.0",
  "description": "Auto-generated wrapper for deployment",
  "scripts": {
    "start": "cd frontend && node .next/standalone/server.js",
    "build": "cd frontend && npm install && npm run build",
    "postinstall": "cd frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}